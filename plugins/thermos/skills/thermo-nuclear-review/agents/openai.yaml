interface:
  display_name: "Thermo Nuclear Review"
  short_description: "Run the requested code review workflow"
  default_prompt: "Use $thermo-nuclear-review in this workspace."
policy:
  allow_implicit_invocation: false
