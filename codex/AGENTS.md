# Starting implementation

Before starting or resuming implementation in a Git repository, invoke `$start-work` to discover existing work, choose the base, and prepare a separate worktree for each new task. Resume an existing task in its worktree when appropriate. Discussion and planning alone must not create branches or worktrees. Explicit `$start-work` invocation is also supported.
